import {test} from 'tap'
import * as projectManagement from '../src/modules/projectmanagement'
test("check", (t) => {
    t.test('works', (t) => {
        t.ok(true)
        t.end()
    })
    t.end()
})